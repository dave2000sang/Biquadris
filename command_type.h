#ifndef COMMAND_TYPE
#define COMMAND_TYPE

enum class CommandType {left, right, down, clockwise, counterclockwise, drop, levelup, leveldown, norandom, random, I, J, L, O, S, Z, T, sequence, restart, nomatch, macro};

#endif
